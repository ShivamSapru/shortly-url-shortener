DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
BASE_URL=http://localhost:3000
PORT=3000
NODE_ENV=development
# Tests truncate tables — use a throwaway Neon branch, never production.
TEST_DATABASE_URL=
